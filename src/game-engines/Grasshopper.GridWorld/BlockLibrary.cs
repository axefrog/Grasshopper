namespace Grasshopper.GridWorld
{
	/// <summary>
	/// A block library is like the 3D version of a tileset. Each block is generally designed to be static
	/// in mesh composition, with instances of blocks used to compose a scene. A single block can have
	/// multiple internal models, and those can have individual transformations applied if desired, but their
	/// composition should generall remain static.
	/// </summary>
	public class BlockLibrary
	{
	}
}